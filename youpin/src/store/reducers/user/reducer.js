const initial = {
    isLogin:false,
    baseInfo:null
}

 const userReducer = (state = initial, action ) => {
    switch (action.key) {
        
            
            
    
        default:
            return state 
    }
}
export default userReducer